<!DOCTYPE html>
<html>
<head>
    <title><%= yield(:title) %> | Beast Games App</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<header class="navbar navbar-fixed-top">
           <nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand", id: "logo" href='/static_pages/home'>Beast Games</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
            <li><%= link_to "Home",  '/static_pages/home' %></li>
            <li><%= link_to "Games",  '/games' %></li>
            <li><%= link_to "Charts", '/static_pages/charts' %></li>
            <li><%= link_to "Tweets", '/tweets' %></li>
            <li><%= link_to "Buy Games", '/products' %></li>
            
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
     <%= form_tag(games_path, method: :get) do %>
     <%= text_field_tag :name, params[:name] %>
     <%= submit_tag 'Search Games', name: nil %>
     <% end %>
        </div>
       
      <ul class="nav navbar-nav navbar-right">
       <li class="active cart"><%= render 'layouts/cart' %></li>
       <li><% if user_signed_in? %><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
        <li><% else %><%= link_to('login', new_user_session_path) %>
        <li><%= link_to "Admin", '/admins/sign_in' %></li>
        <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
    </header>
    <body>
    <div class="container">
       <% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>
      


<%= yield %>

</body>
</html>
